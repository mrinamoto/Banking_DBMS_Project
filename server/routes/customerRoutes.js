const express=require('express');const {getAllCustomers,addCustomer,updateCustomer,setCustomerStatus}=require('../controllers/customerController');const {authenticate,allowRoles}=require('../middleware/auth');const router=express.Router();router.use(authenticate);router.get('/',getAllCustomers);router.post('/',allowRoles('ADMIN','MANAGER','EMPLOYEE'),addCustomer);router.patch('/:id',updateCustomer);router.patch('/:id/status',allowRoles('ADMIN','MANAGER'),setCustomerStatus);module.exports=router;
