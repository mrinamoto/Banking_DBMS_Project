import {useState} from "react";import {Landmark,Eye,EyeOff} from "lucide-react";import {useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";import {messageFrom} from "../services/api";import {ErrorBox} from "../components/UI";
export default function Login(){const [form,setForm]=useState({username:'',password:''});const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const auth=useAuth();const navigate=useNavigate();async function submit(e){e.preventDefault();setBusy(true);setError('');try{await auth.login(form.username,form.password);navigate('/')}catch(err){setError(messageFrom(err))}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-slate-950 p-4"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-2xl"><div className="mb-7 flex items-center gap-3"><span className="rounded-xl bg-emerald-600 p-3 text-white"><Landmark/></span><div><h1 className="text-xl font-bold">Smart Banking</h1><p className="text-sm text-slate-500">Academic management system</p></div></div><ErrorBox message={error}/><form onSubmit={submit} className="space-y-4"><label className="field">Username<input autoComplete="username" required value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label className="field">Password<span className="relative"><input className="w-full" type={show?'text':'password'} autoComplete="current-password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" aria-label="Toggle password visibility" className="absolute right-3 top-2.5 text-slate-500" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></span></label><button className="btn-primary w-full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><p className="mt-6 text-xs text-slate-500">Use a demo account created with the documented password-hash command. No password is stored in this interface.</p></section></main>}
