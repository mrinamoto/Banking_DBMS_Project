{
  "scripts": {
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:lint": "npm --prefix client run lint",
    "client:test": "node --test client/test/depositSuiteState.test.js",
    "server:start": "npm --prefix server start",
    "db:test": "npm --prefix server run db:test",
    "db:doctor": "npm --prefix server run db:doctor",
    "db:build-worksheets": "node scripts/build-worksheets.js",
    "db:check-worksheets": "node scripts/build-worksheets.js --check",
    "server:check": "npm --prefix server run check",
    "server:test": "npm --prefix server test"
  },
  "devDependencies": {
    "enhanced-resolve": "^5.24.3"
  },
  "dependencies": {
    "oracledb": "^7.0.1"
  }
}
